import type {CollatzRendererUpdateConfig} from "../draw";

export const outputFiveFriendly = (baseLimit?: number, updateHandler?: (config: CollatzRendererUpdateConfig) => void): void => {
    // ...
};